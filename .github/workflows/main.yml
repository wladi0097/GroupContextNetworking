name: C++

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cmake .
    - name: build
      run: cmake --build . --target all
    - name: test
      run: cd test && ./Catch_tests_run
